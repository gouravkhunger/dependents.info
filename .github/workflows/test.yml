name: Test Action

on:
  push:
    branches: [main]

  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run custom action
        uses: ./
        with:
          repo: "gouravkhunger/jekyll-auto-authors"
